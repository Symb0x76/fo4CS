[pixelHunt]
type=PS
; asmHash intentionally unset.
; shader intentionally disabled.
active=false
priority=0

; LightLimitFix sentinel only. Do not enable this entry by filling asmHash,
; uncommenting shader=, or setting active=true. FO4 LLF follows the Skyrim
; Community Shaders engine-lighting path: validate the internal point-light
; callsite, collect BSRenderPass scene lights, prove strict-light CB data,
; then bind b3/t35-t37 only in the verified lighting shader path.
; Standalone LLF PS templates are support files only, not active PreNG proof.
;
; ShaderDB / dump metadata may support shader-path investigation later, but
; this placeholder must remain inactive unless a separate, validated non-LLF
; shader replacement task explicitly owns it.
